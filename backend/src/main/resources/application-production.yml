spring:
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: false
  flyway:
    enabled: true

server:
  forward-headers-strategy: framework

logging:
  level:
    org.springframework.security: WARN
